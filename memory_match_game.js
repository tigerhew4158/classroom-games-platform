
    const I18N = {
      zh: {
        title: '翻牌记忆配对赛',
        subtitle: '老师投影即可玩：翻开两张牌，找出正确配对。',
        fullscreen: '全屏',
        soundOn: '音效：开',
        soundOff: '音效：关',
        teacherSettings: '老师设定',
        showSettings: '显示老师设定',
        teamCount: '分组数量',
        timeLimit: '游戏时间',
        noLimit: '不限时',
        pairCount: '牌组数量',
        correctTurn: '答对后',
        wrongTurn: '答错后',
        stayTurn: '继续作答',
        nextTurn: '换下一组',
        teamNames: '队伍名称',
        teamNamesHint: '用逗号分隔；选择几组就会使用前几个名称。',
        questionBank: '配对题库',
        bankHint: '每一行一组配对：左边文字,右边文字。例：苹果,apple',
        loadSample: '载入示例',
        exportTxt: '汇出 TXT',
        importTxt: '汇入 TXT',
        reshuffle: '重新洗牌',
        startGame: '开始游戏',
        resetGame: '重置',
        offlineNote: '离线可用：直接双击 index.html，即可在课室投影使用。',
        currentTurn: '当前回合',
        timer: '倒数时间',
        matchedPairs: '已配对',
        readyMsg: '请先在左侧设定题库，然后点击“开始游戏”。',
        selectFirst: '请选择第一张牌。',
        selectSecond: '请再选择一张牌，看看是否配对。',
        match: '配对成功！加 1 分。',
        noMatch: '没有配对成功，记住位置，下次再试。',
        gameStarted: '游戏开始！请当前队伍翻开两张牌。',
        gameReset: '游戏已重置。',
        notEnoughPairs: '题库配对不足，请至少输入 6 组配对。',
        gameOver: '游戏结束',
        allMatched: '全部配对完成！',
        timeUp: '时间到！',
        resultDesc: '成绩如下：',
        winner: '获胜',
        draw: '平手',
        playAgain: '再玩一次',
        close: '关闭',
        cardA: 'A',
        cardB: 'B',
        teamsDefault: '红队,蓝队,绿队,紫队',
        sampleBank: '苹果,apple\n香蕉,banana\n老师,teacher\n学生,student\n人工智能,AI\n吉隆坡,Kuala Lumpur\n早安,selamat pagi\n谢谢,thank you\n中央处理器,CPU\n光合作用,photosynthesis\n安全密码,strong password\n网络安全,cybersecurity'
      },
      en: {
        title: 'Memory Match Classroom Game',
        subtitle: 'Project it in class: flip two cards and find the correct match.',
        fullscreen: 'Fullscreen',
        soundOn: 'Sound: On',
        soundOff: 'Sound: Off',
        teacherSettings: 'Teacher Settings',
        showSettings: 'Show Teacher Settings',
        teamCount: 'Number of Teams',
        timeLimit: 'Game Time',
        noLimit: 'No limit',
        pairCount: 'Number of Pairs',
        correctTurn: 'After Correct Match',
        wrongTurn: 'After Wrong Match',
        stayTurn: 'Same team continues',
        nextTurn: 'Next team',
        teamNames: 'Team Names',
        teamNamesHint: 'Separate by commas; the game uses the first few names based on team count.',
        questionBank: 'Matching Bank',
        bankHint: 'One pair per line: left text,right text. Example: apple,苹果',
        loadSample: 'Load Sample',
        exportTxt: 'Export TXT',
        importTxt: 'Import TXT',
        reshuffle: 'Reshuffle',
        startGame: 'Start Game',
        resetGame: 'Reset',
        offlineNote: 'Works offline: double-click index.html and project it in class.',
        currentTurn: 'Current Turn',
        timer: 'Timer',
        matchedPairs: 'Matched Pairs',
        readyMsg: 'Set the matching bank on the left, then click “Start Game”.',
        selectFirst: 'Choose the first card.',
        selectSecond: 'Choose one more card and check if it matches.',
        match: 'Correct match! +1 point.',
        noMatch: 'Not a match. Remember the positions and try again.',
        gameStarted: 'Game started! Current team flips two cards.',
        gameReset: 'Game has been reset.',
        notEnoughPairs: 'Not enough pairs. Please enter at least 6 pairs.',
        gameOver: 'Game Over',
        allMatched: 'All pairs matched!',
        timeUp: 'Time is up!',
        resultDesc: 'Final results:',
        winner: 'Winner',
        draw: 'Draw',
        playAgain: 'Play Again',
        close: 'Close',
        cardA: 'A',
        cardB: 'B',
        teamsDefault: 'Red Team,Blue Team,Green Team,Purple Team',
        sampleBank: 'apple,苹果\nbanana,香蕉\nteacher,老师\nstudent,学生\nAI,人工智能\nKuala Lumpur,吉隆坡\nselamat pagi,早安\nthank you,谢谢\nCPU,中央处理器\nphotosynthesis,光合作用\nstrong password,安全密码\ncybersecurity,网络安全'
      },
      ms: {
        title: 'Permainan Padanan Kad Memori',
        subtitle: 'Paparkan di kelas: buka dua kad dan cari pasangan yang betul.',
        fullscreen: 'Skrin Penuh',
        soundOn: 'Bunyi: Buka',
        soundOff: 'Bunyi: Tutup',
        teacherSettings: 'Tetapan Guru',
        showSettings: 'Tunjuk Tetapan Guru',
        teamCount: 'Bilangan Kumpulan',
        timeLimit: 'Masa Permainan',
        noLimit: 'Tiada had',
        pairCount: 'Bilangan Pasangan',
        correctTurn: 'Selepas Betul',
        wrongTurn: 'Selepas Salah',
        stayTurn: 'Kumpulan sama terus jawab',
        nextTurn: 'Giliran kumpulan seterusnya',
        teamNames: 'Nama Kumpulan',
        teamNamesHint: 'Pisahkan dengan koma; permainan akan guna nama mengikut bilangan kumpulan.',
        questionBank: 'Bank Padanan',
        bankHint: 'Satu pasangan setiap baris: teks kiri,teks kanan. Contoh: epal,apple',
        loadSample: 'Muat Sampel',
        exportTxt: 'Eksport TXT',
        importTxt: 'Import TXT',
        reshuffle: 'Kocok Semula',
        startGame: 'Mula Permainan',
        resetGame: 'Set Semula',
        offlineNote: 'Boleh guna tanpa internet: klik dua kali index.html dan paparkan di kelas.',
        currentTurn: 'Giliran Semasa',
        timer: 'Masa',
        matchedPairs: 'Pasangan Berjaya',
        readyMsg: 'Tetapkan bank padanan di sebelah kiri, kemudian tekan “Mula Permainan”.',
        selectFirst: 'Pilih kad pertama.',
        selectSecond: 'Pilih satu lagi kad dan lihat sama ada ia sepadan.',
        match: 'Padanan betul! +1 markah.',
        noMatch: 'Tidak sepadan. Ingat kedudukan kad dan cuba lagi.',
        gameStarted: 'Permainan bermula! Kumpulan semasa membuka dua kad.',
        gameReset: 'Permainan telah diset semula.',
        notEnoughPairs: 'Pasangan tidak mencukupi. Sila masukkan sekurang-kurangnya 6 pasangan.',
        gameOver: 'Permainan Tamat',
        allMatched: 'Semua pasangan berjaya dipadankan!',
        timeUp: 'Masa tamat!',
        resultDesc: 'Keputusan akhir:',
        winner: 'Pemenang',
        draw: 'Seri',
        playAgain: 'Main Lagi',
        close: 'Tutup',
        cardA: 'A',
        cardB: 'B',
        teamsDefault: 'Kumpulan Merah,Kumpulan Biru,Kumpulan Hijau,Kumpulan Ungu',
        sampleBank: 'epal,apple\npisang,banana\nguru,teacher\nmurid,student\nkecerdasan buatan,AI\nKuala Lumpur,吉隆坡\nselamat pagi,早安\nterima kasih,thank you\nCPU,unit pemprosesan pusat\nfotosintesis,photosynthesis\nkata laluan kuat,strong password\nkeselamatan siber,cybersecurity'
      }
    };

    const COLORS = ['red', 'blue', 'green', 'purple'];
    const COLOR_EMOJI = ['🔴', '🔵', '🟢', '🟣'];

    const state = {
      lang: 'zh',
      sound: true,
      running: false,
      teams: [],
      currentTeam: 0,
      cards: [],
      flipped: [],
      lock: false,
      matchedPairs: 0,
      totalPairs: 8,
      secondsLeft: 300,
      timerId: null,
      bankPairs: []
    };

    const $ = (id) => document.getElementById(id);
    const t = (key) => (I18N[state.lang] && I18N[state.lang][key]) || I18N.zh[key] || key;

    function saveSettings() {
      const data = {
        lang: $('lang').value,
        teamCount: $('teamCount').value,
        timeLimit: $('timeLimit').value,
        pairCount: $('pairCount').value,
        correctTurn: $('correctTurn').value,
        wrongTurn: $('wrongTurn').value,
        teamNames: $('teamNames').value,
        bank: $('bank').value,
        sound: state.sound
      };
      localStorage.setItem('memoryMatchSettings', JSON.stringify(data));
    }

    function loadSettings() {
      const raw = localStorage.getItem('memoryMatchSettings');
      if (!raw) {
        $('bank').value = I18N.zh.sampleBank;
        return;
      }
      try {
        const data = JSON.parse(raw);
        $('lang').value = data.lang || 'zh';
        state.lang = $('lang').value;
        $('teamCount').value = data.teamCount || '2';
        $('timeLimit').value = data.timeLimit || '300';
        $('pairCount').value = data.pairCount || '8';
        $('correctTurn').value = data.correctTurn || 'stay';
        $('wrongTurn').value = data.wrongTurn || 'next';
        $('teamNames').value = data.teamNames || t('teamsDefault');
        $('bank').value = data.bank || t('sampleBank');
        state.sound = data.sound !== false;
      } catch (err) {
        $('bank').value = I18N.zh.sampleBank;
      }
    }

    function applyLanguage() {
      document.documentElement.lang = state.lang === 'zh' ? 'zh-CN' : state.lang;
      document.querySelectorAll('[data-i18n]').forEach(el => {
        const key = el.getAttribute('data-i18n');
        if (I18N[state.lang][key]) el.textContent = I18N[state.lang][key];
      });
      $('soundBtn').textContent = state.sound ? t('soundOn') : t('soundOff');
      if (!state.running && !$('teamNames').dataset.customized) {
        $('teamNames').value = t('teamsDefault');
      }
      updateStaticTexts();
      saveSettings();
    }

    function updateStaticTexts() {
      updateTimerText();
      $('matchedText').textContent = `${state.matchedPairs} / ${state.totalPairs}`;
      if (!state.running) $('turnText').textContent = '—';
    }

    function parseBank() {
      return $('bank').value
        .split(/\r?\n/)
        .map(line => line.trim())
        .filter(Boolean)
        .map(line => {
          let parts = line.includes('\t') ? line.split('\t') : line.split(',');
          if (parts.length < 2) parts = line.split('|');
          const left = (parts[0] || '').trim();
          const right = parts.slice(1).join(',').trim();
          return { left, right };
        })
        .filter(p => p.left && p.right);
    }

    function shuffle(arr) {
      const a = [...arr];
      for (let i = a.length - 1; i > 0; i--) {
        const j = Math.floor(Math.random() * (i + 1));
        [a[i], a[j]] = [a[j], a[i]];
      }
      return a;
    }

    function setupTeams() {
      const count = Number($('teamCount').value);
      const names = $('teamNames').value.split(',').map(x => x.trim()).filter(Boolean);
      const defaults = t('teamsDefault').split(',');
      state.teams = Array.from({ length: count }, (_, i) => ({
        name: names[i] || defaults[i] || `Team ${i + 1}`,
        score: 0,
        attempts: 0,
        matches: 0,
        color: COLORS[i],
        emoji: COLOR_EMOJI[i]
      }));
      state.currentTeam = 0;
      renderTeams();
      updateTurn();
    }

    function renderTeams() {
      const box = $('teams');
      box.className = `teams cols-${state.teams.length || Number($('teamCount').value)}`;
      box.innerHTML = '';
      state.teams.forEach((team, idx) => {
        const div = document.createElement('div');
        div.className = `team ${team.color} ${idx === state.currentTeam && state.running ? 'active' : ''}`;
        div.innerHTML = `
          <div class="team-top">
            <div class="team-name">${team.emoji} ${escapeHtml(team.name)}</div>
            <div class="score">${team.score}</div>
          </div>
          <div class="team-meta">${t('matchedPairs')}: ${team.matches} · ${state.lang === 'ms' ? 'Cubaan' : state.lang === 'en' ? 'Attempts' : '尝试'}: ${team.attempts}</div>
        `;
        box.appendChild(div);
      });
    }

    function createCards() {
      const pairs = shuffle(parseBank());
      const requested = Number($('pairCount').value);
      if (pairs.length < 6) {
        showMessage(t('notEnoughPairs'));
        beep('wrong');
        return false;
      }
      const selected = pairs.slice(0, Math.min(requested, pairs.length));
      state.totalPairs = selected.length;
      state.matchedPairs = 0;
      const cards = [];
      selected.forEach((pair, index) => {
        cards.push({ id: `${index}`, text: pair.left, kind: t('cardA'), matched: false });
        cards.push({ id: `${index}`, text: pair.right, kind: t('cardB'), matched: false });
      });
      state.cards = shuffle(cards).map((card, idx) => ({ ...card, uid: `card-${idx}` }));
      return true;
    }

    function renderBoard() {
      const board = $('board');
      board.className = `board size-${state.cards.length}`;
      board.innerHTML = '';
      state.cards.forEach((card, index) => {
        const btn = document.createElement('button');
        btn.className = `card ${card.matched ? 'matched' : ''}`;
        btn.setAttribute('aria-label', `card ${index + 1}`);
        btn.dataset.uid = card.uid;
        btn.onclick = () => flipCard(card.uid);
        btn.innerHTML = `
          <div class="card-inner">
            <div class="card-front">
              <div class="card-no">${index + 1}</div>
              <div class="card-symbol">✦</div>
            </div>
            <div class="card-back">
              <div class="card-kind">${escapeHtml(card.kind)}</div>
              <div class="card-text">${escapeHtml(card.text)}</div>
            </div>
          </div>
        `;
        board.appendChild(btn);
      });
      $('matchedText').textContent = `${state.matchedPairs} / ${state.totalPairs}`;
    }

    function flipCard(uid) {
      if (!state.running || state.lock) return;
      const card = state.cards.find(c => c.uid === uid);
      if (!card || card.matched) return;
      const el = document.querySelector(`[data-uid="${uid}"]`);
      if (!el || el.classList.contains('flipped')) return;
      if (state.flipped.length >= 2) return;

      el.classList.add('flipped');
      state.flipped.push(card);
      beep('flip');

      if (state.flipped.length === 1) {
        showMessage(t('selectSecond'));
      } else {
        checkMatch();
      }
    }

    function checkMatch() {
      state.lock = true;
      const [a, b] = state.flipped;
      const team = state.teams[state.currentTeam];
      team.attempts++;

      if (a.id === b.id && a.uid !== b.uid) {
        a.matched = true;
        b.matched = true;
        team.score++;
        team.matches++;
        state.matchedPairs++;
        document.querySelector(`[data-uid="${a.uid}"]`).classList.add('matched');
        document.querySelector(`[data-uid="${b.uid}"]`).classList.add('matched');
        showMessage(t('match'));
        beep('correct');
        state.flipped = [];
        state.lock = false;
        if ($('correctTurn').value === 'next') nextTeam();
        updateAll();
        if (state.matchedPairs >= state.totalPairs) endGame('allMatched');
      } else {
        showMessage(t('noMatch'));
        beep('wrong');
        setTimeout(() => {
          document.querySelector(`[data-uid="${a.uid}"]`)?.classList.remove('flipped');
          document.querySelector(`[data-uid="${b.uid}"]`)?.classList.remove('flipped');
          state.flipped = [];
          state.lock = false;
          if ($('wrongTurn').value === 'next') nextTeam();
          updateAll();
        }, 1050);
      }
    }

    function nextTeam() {
      state.currentTeam = (state.currentTeam + 1) % state.teams.length;
      showMessage(`${state.teams[state.currentTeam].emoji} ${state.teams[state.currentTeam].name}: ${t('selectFirst')}`);
      updateTurn();
    }

    function updateTurn() {
      if (state.running && state.teams[state.currentTeam]) {
        $('turnText').textContent = `${state.teams[state.currentTeam].emoji} ${state.teams[state.currentTeam].name}`;
      } else {
        $('turnText').textContent = '—';
      }
      renderTeams();
    }

    function updateAll() {
      $('matchedText').textContent = `${state.matchedPairs} / ${state.totalPairs}`;
      updateTurn();
      saveSettings();
    }

    function showMessage(msg) {
      $('messageText').textContent = msg;
    }

    function enterGameOnlyMode() {
      document.body.classList.add('game-only');
    }

    function exitGameOnlyMode() {
      document.body.classList.remove('game-only');
    }

    function startGame() {
      stopTimer();
      saveSettings();
      setupTeams();
      if (!createCards()) return;
      enterGameOnlyMode();
      state.running = true;
      state.lock = false;
      state.flipped = [];
      state.secondsLeft = Number($('timeLimit').value);
      renderBoard();
      updateAll();
      showMessage(t('gameStarted'));
      if (state.secondsLeft > 0) {
        updateTimerText();
        state.timerId = setInterval(() => {
          state.secondsLeft--;
          updateTimerText();
          if (state.secondsLeft <= 0) endGame('timeUp');
        }, 1000);
      } else {
        updateTimerText();
      }
      $('endModal').classList.remove('show');
      beep('start');
    }

    function resetGame() {
      stopTimer();
      exitGameOnlyMode();
      state.running = false;
      state.lock = false;
      state.flipped = [];
      state.matchedPairs = 0;
      state.secondsLeft = Number($('timeLimit').value);
      state.cards = [];
      setupTeams();
      renderEmptyBoard();
      updateTimerText();
      $('matchedText').textContent = `0 / ${Number($('pairCount').value)}`;
      showMessage(t('gameReset'));
      $('endModal').classList.remove('show');
      saveSettings();
    }

    function renderEmptyBoard() {
      const count = Number($('pairCount').value) * 2;
      state.cards = Array.from({ length: count }, (_, i) => ({ uid: `empty-${i}`, text: '', id: `empty-${i}` }));
      const board = $('board');
      board.className = `board size-${count}`;
      board.innerHTML = '';
      for (let i = 0; i < count; i++) {
        const card = document.createElement('button');
        card.className = 'card';
        card.disabled = true;
        card.innerHTML = `
          <div class="card-inner">
            <div class="card-front">
              <div class="card-no">${i + 1}</div>
              <div class="card-symbol">✦</div>
            </div>
            <div class="card-back"><div class="card-text"></div></div>
          </div>
        `;
        board.appendChild(card);
      }
    }

    function reshuffle() {
      if (!state.running) {
        startGame();
      } else {
        if (!confirm(state.lang === 'zh' ? '要重新洗牌并重新开始吗？' : state.lang === 'ms' ? 'Kocok semula dan mula semula?' : 'Reshuffle and restart?')) return;
        startGame();
      }
    }

    function endGame(reasonKey) {
      if (!state.running) return;
      state.running = false;
      state.lock = true;
      stopTimer();
      updateTurn();
      showMessage(reasonKey === 'timeUp' ? t('timeUp') : t('allMatched'));
      beep('end');
      const sorted = [...state.teams].sort((a, b) => b.score - a.score || a.attempts - b.attempts);
      const topScore = sorted[0]?.score ?? 0;
      const winners = sorted.filter(team => team.score === topScore);
      $('endTitle').textContent = winners.length > 1 ? `${t('gameOver')} · ${t('draw')}` : `${t('gameOver')} · ${winners[0].name} ${t('winner')}`;
      $('endDesc').textContent = `${reasonKey === 'timeUp' ? t('timeUp') : t('allMatched')} ${t('resultDesc')}`;
      $('winnerList').innerHTML = sorted.map((team, i) => `
        <div class="winner-row">
          <span>${i + 1}. ${team.emoji} ${escapeHtml(team.name)}</span>
          <span>${team.score}</span>
        </div>
      `).join('');
      $('endModal').classList.add('show');
    }

    function stopTimer() {
      if (state.timerId) clearInterval(state.timerId);
      state.timerId = null;
    }

    function updateTimerText() {
      const val = Number($('timeLimit').value);
      if (val === 0 && !state.running) {
        $('timerText').textContent = '∞';
        return;
      }
      if (state.running && Number($('timeLimit').value) === 0) {
        $('timerText').textContent = '∞';
        return;
      }
      const seconds = state.running ? state.secondsLeft : Number($('timeLimit').value);
      if (seconds === 0) {
        $('timerText').textContent = '∞';
        return;
      }
      const m = Math.floor(seconds / 60).toString().padStart(2, '0');
      const s = Math.floor(seconds % 60).toString().padStart(2, '0');
      $('timerText').textContent = `${m}:${s}`;
    }

    function exportTxt() {
      const blob = new Blob([$('bank').value], { type: 'text/plain;charset=utf-8' });
      const url = URL.createObjectURL(blob);
      const a = document.createElement('a');
      a.href = url;
      a.download = 'memory-match-bank.txt';
      a.click();
      URL.revokeObjectURL(url);
    }

    function importTxt(file) {
      const reader = new FileReader();
      reader.onload = () => {
        $('bank').value = String(reader.result || '');
        saveSettings();
      };
      reader.readAsText(file, 'utf-8');
    }

    function beep(type) {
      if (!state.sound) return;
      try {
        const AudioContext = window.AudioContext || window.webkitAudioContext;
        const ctx = new AudioContext();
        const osc = ctx.createOscillator();
        const gain = ctx.createGain();
        const now = ctx.currentTime;
        const map = {
          flip: [520, .045],
          correct: [760, .12],
          wrong: [180, .16],
          start: [620, .12],
          end: [420, .18]
        };
        const [freq, dur] = map[type] || map.flip;
        osc.frequency.setValueAtTime(freq, now);
        if (type === 'correct') osc.frequency.exponentialRampToValueAtTime(980, now + dur);
        if (type === 'wrong') osc.frequency.exponentialRampToValueAtTime(110, now + dur);
        gain.gain.setValueAtTime(.001, now);
        gain.gain.exponentialRampToValueAtTime(.12, now + .01);
        gain.gain.exponentialRampToValueAtTime(.001, now + dur);
        osc.connect(gain);
        gain.connect(ctx.destination);
        osc.start(now);
        osc.stop(now + dur + .02);
        setTimeout(() => ctx.close(), (dur + .08) * 1000);
      } catch (e) {}
    }

    function escapeHtml(str) {
      return String(str)
        .replaceAll('&', '&amp;')
        .replaceAll('<', '&lt;')
        .replaceAll('>', '&gt;')
        .replaceAll('"', '&quot;')
        .replaceAll("'", '&#039;');
    }

    function initEvents() {
      $('lang').addEventListener('change', () => {
        state.lang = $('lang').value;
        applyLanguage();
      });
      ['teamCount', 'timeLimit', 'pairCount', 'correctTurn', 'wrongTurn', 'bank'].forEach(id => {
        $(id).addEventListener('change', () => {
          saveSettings();
          if (!state.running && (id === 'pairCount' || id === 'timeLimit')) resetGame();
        });
      });
      $('teamNames').addEventListener('input', () => {
        $('teamNames').dataset.customized = '1';
        saveSettings();
        if (!state.running) setupTeams();
      });
      $('startBtn').onclick = startGame;
      $('resetBtn').onclick = resetGame;
      $('shuffleBtn').onclick = reshuffle;
      $('showSettingsBtn').onclick = exitGameOnlyMode;
      $('gameResetBtn').onclick = resetGame;
      $('gameReshuffleBtn').onclick = reshuffle;
      $('sampleBtn').onclick = () => {
        $('bank').value = t('sampleBank');
        saveSettings();
        showMessage(state.lang === 'zh' ? '已载入示例题库。' : state.lang === 'ms' ? 'Bank sampel telah dimuatkan.' : 'Sample bank loaded.');
      };
      $('exportBtn').onclick = exportTxt;
      $('importFile').addEventListener('change', (e) => {
        const file = e.target.files && e.target.files[0];
        if (file) importTxt(file);
        e.target.value = '';
      });
      $('fullscreenBtn').onclick = () => {
        if (!document.fullscreenElement) document.documentElement.requestFullscreen?.();
        else document.exitFullscreen?.();
      };
      $('soundBtn').onclick = () => {
        state.sound = !state.sound;
        $('soundBtn').textContent = state.sound ? t('soundOn') : t('soundOff');
        saveSettings();
      };
      $('playAgainBtn').onclick = startGame;
      $('closeModalBtn').onclick = () => $('endModal').classList.remove('show');
    }

    function init() {
      loadSettings();
      initEvents();
      applyLanguage();
      setupTeams();
      resetGame();
      showMessage(t('readyMsg'));
    }

    init();
  